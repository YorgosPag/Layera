/**
 * DesktopGeoMap.tsx - ΜΟΝΟ για υπολογιστές (desktop)
 * Εδώ θα γράφουμε μόνο τα UI elements για desktop συσκευές
 */

import React from 'react';

export interface DesktopGeoMapProps {
  // Props που χρειάζονται για το desktop UI
}

/**
 * Desktop-specific UI elements για το GeoMap
 * Περιλαμβάνει: desktop navigation, larger buttons, desktop-specific features κλπ
 */
export const DesktopGeoMap: React.FC<DesktopGeoMapProps> = () => {
  return (
    <>
      {/* Desktop-specific UI elements θα προστεθούν εδώ */}
      {/* Π.χ. larger toolbars, desktop menus, κλπ */}

      {/* Προς το παρόν άδειο - θα προσθέτουμε κατά περίπτωση */}
    </>
  );
};